{
  "name": "sellernerve-api",
  "version": "0.1.0",
  "private": true,
  "description": "SellerNerve live-proof API. WB-first. Telegram-first. Minimal Express app with /health/live and /health/ready (Postgres readiness via SELECT 1).",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.13.1"
  }
}
